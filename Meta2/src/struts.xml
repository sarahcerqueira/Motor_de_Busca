<?xml version="1.0" encoding="UTF-8"?>

<!-- The core configuration file for the framework is the default (struts.xml) file
and should reside on the classpath of the webapp (generally /WEB-INF/classes). -->

<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

  <!-- devMode equals debug information and reload everything for every request -->
  <constant name="struts.devMode" value="true" />
  <constant name="struts.ui.theme" value="simple" />

  <package name="Meta2" extends="struts-default">
	
	<default-action-ref name="NewFile" />
	
	<global-results>
		<result name="error">/error.jsp</result>
	</global-results>

	<!-- all exceptions not caught by the application will lead to error.jsp -->
	<global-exception-mappings>
		<exception-mapping exception="java.lang.Exception" result="error" />
	</global-exception-mappings>

	<!-- 'NewFile' action leads to the view provided by index.jsp -->
    <action name="NewFile">
      <result>/NewFile.jsp</result>
    </action>

	<!-- 'primes' action calls 'execute' in 'PrimesAction', followed by the view in 'primes.jsp' -->
    <action name="NewFile" class="NewFile.action.NewFileAction" method="execute">
      <result name="success">/NewFile.jsp</result>
    </action>

  </package>

</struts>